data/*.sqlite
data/*.sqlite-*

# Per-machine Claude Code settings (locally granted permissions).
# .claude/settings.json, by contrast, is meant to be shared if it ever exists.
.claude/settings.local.json
