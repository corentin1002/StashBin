data/*.sqlite
data/*.sqlite-*

# Réglages Claude Code propres à chaque poste (permissions accordées localement).
# .claude/settings.json, lui, a vocation à être partagé s'il vient à exister.
.claude/settings.local.json
